{
  "name": "cloud-backend",
  "version": "1.0.0",
  "description": "ClickFlash Cloudflare Worker Backend for D1, R2, Auth, and Stripe",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clickflash/licensing": "workspace:*",
    "hono": "^4.0.0",
    "stripe": "^15.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
