# Phase 1: Stripe & Billing Tasks

- [x] Create D1 migration to add billing fields (`stripe_customer_id`, `stripe_subscription_id`, `plan_tier`) to the `users` table.
- [x] Install `stripe` dependency in `management-worker`.
- [x] Implement `POST /api/billing/checkout` endpoint in `management-worker`.
- [x] Implement `POST /api/billing/webhook` endpoint in `management-worker`.
- [x] Update `apps/management` frontend (`PricingTable.tsx`) to trigger checkout.

# Phase 2: Gallery App (Customer Purchasing Flow)

- [x] Create Zustand store (`useCartStore.ts`) to manage selected photos and print sizes in `apps/gallery`.
- [x] Add floating Shopping Cart icon and a Checkout Sidebar UI components in `apps/gallery`.
- [x] Add "Add to Cart" buttons over individual photos in the gallery grid in `apps/gallery`.
- [x] Implement `POST /api/gallery/checkout` in `management-worker` for customer purchases.
- [x] Implement post-purchase fulfillment logic in `management-worker` Stripe webhook.
