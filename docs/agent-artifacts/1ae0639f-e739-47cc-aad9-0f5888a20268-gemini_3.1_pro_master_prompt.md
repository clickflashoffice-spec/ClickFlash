# 🌟 Gemini 3.1 Pro Master Prompt: Deep Source Code Scan & Ecosystem Build

**Copy and paste this prompt into Gemini 3.1 Pro. It is specifically engineered to leverage Gemini's massive context window, deep thinking, and online research capabilities—commanding it to scan your raw source code from zero rather than relying on any pre-made reports.**

***

```markdown
<USER_REQUEST>
**Goal:** You are Gemini 3.1 Pro acting as Principal Systems Architect, SDET, and Full-Stack Lead. Your objective is to perform a 100% complete, zero-trust audit, refactor, feature build, 9-layer QA gauntlet, and production release for the entire ClickFlash Photography Ecosystem.

### 🧠 Gemini 3.1 Pro Core Execution Directives

1. **ZERO-TRUST RAW SOURCE CODE SCANNING (DO NOT USE READY REPORTS):**
   - Do not trust or rely on any existing summary markdown files or pre-made reports.
   - Use your file inspection and grep search tools to **read the actual raw `.tsx`, `.ts`, `.rs`, and schema files from zero**.
   - Audit every single route, component, API endpoint, and database query by examining the actual codebase syntax.

2. **DEEP THINKING & ONLINE RESEARCH:**
   - Engage deep reasoning before modifying architectural boundaries between offline apps (`master`, `touch`) and cloud apps (`management`, `gallery`).
   - Use online web search (`search_web`) to check the latest 2026 docs and security advisories for **React 19, Next.js 15/16, Tailwind CSS 4, Cloudflare Workers/D1/R2, Electron, and Tauri Rust bindings**.

3. **UNCOMPROMISING 100% CUSTOM MANDATE (NO SUBSCRIPTIONS):**
   - NEVER introduce paid third-party SaaS subscriptions (No Vercel, Auth0, Clerk, Pusher, Algolia, OpenAI, Adobe, or paid analytics).
   - Build custom Auth (JWT/Magic Links), custom WebSockets, custom analytics, and custom UI components.

---

### 🔍 Scope & Direct Codebase Verification Checklist

Scan the source code from zero across all 9 components:

#### **A. Monorepo Apps (`apps/`) & Core Packages (`packages/`)**
1. **`apps/master` (Electron + React 19, Port 8090 | Studio Core):**
   - Scan all route files in `src/`. Verify RBAC (`can("view...")`) and Zod schema validation.
   - **BUILD CUSTOM OFFLINE AUTO EDITOR:** Implement a custom WASM/Canvas image processing engine inside `master` for auto-exposure, color correction, noise reduction, and cropping. Hook it into `BackgroundJobRunner` for non-blocking bulk edits. Store `edit_metadata` in local SQLite and sync to Cloud D1.
   - Audit `ThermalMonitor`, `BackgroundJobRunner`, and the local Express server. Eliminate N+1 SQLite queries.

2. **`apps/touch` (Electron + React 19, Port 8091 | Offline Kiosk):**
   - Scan views (`welcome`, `photos`, `photo-detail`, `order-config`).
   - Polish touch-first UI with Framer Motion transitions. Hard-verify 100% local RFID/Wristband and Face login algorithms directly in code. Ensure `localStorage` cart recovery works across crashes.

3. **`apps/management` (Vite + React 19 + CF Workers/D1 | Enterprise Hub):**
   - Scan all 12 dashboard views. Verify the Global vs. Hotel context state selector.
   - Polish the live Fleet Monitor ping system, custom `Cmd+K` palette, and internal AI Chatbot UI. Optimize Cloudflare D1 queries.

4. **`apps/gallery` (Vite + React 19 + CF Workers/D1/R2/Stripe | Client Portal):**
   - Scan auth routes. Verify custom Passwordless Auth (Magic Link, QR, Email/PIN).
   - Verify Stripe checkout webhooks and custom D1 abandoned cart recovery. Build a native-feeling swipeable lightbox with R2 edge delivery.

5. **`apps/moneytrash` (Next.js 16 + Tauri, Port 3000 | RAW/JPEG Bulk Ingestor):**
   - Read the Rust Tauri command files. Verify multi-threaded chunking and direct R2 upload logic for gigabyte-scale SD card drops.

6. **`apps/website` (Next.js 15 App Router + Tailwind 4, Port 3001):**
   - Read all 14+ route files. Enforce custom dynamic sitemaps, OpenGraph tags, and 100/100 Lighthouse performance.

7. **Shared Packages (`packages/`):**
   - Scan `api`, `validation` (Zod), `logger` (replace all `console.log`), and `ui`.

#### **B. Standalone Infrastructure Tools (`C:\Users\alamo\Desktop\apps\`)**
8. **`license-generator`:** Read source code to verify asymmetric RSA-4096 / Ed25519 signing and strict hardware machine-binding (CPU/MAC address fingerprinting).
9. **`installer`:** Scan packaging scripts. Optimize LZMA/7z compression, silent prerequisite checking, and custom dark-mode setup wizard UI.

---

### 🧪 9-Layer Production QA Gauntlet (Write & Execute)
1. **Unit & API Integration:** Write Vitest/Jest suites for `@clickflash/*` (target 95% coverage).
2. **Web E2E:** Playwright flows for `management`, `gallery`, `website`.
3. **Desktop E2E:** Automated Playwright/Tauri binary tests for `master`, `touch`, `moneytrash`.
4. **Cross-App Sync:** Automate `Touch Cart -> Master WebSocket -> Cloud D1 -> Gallery Store`.
5. **Load & Stress:** `k6` stress test (1,000 requests against Master Express and CF Workers).
6. **Security & Pen-Test:** Scan inputs for SQLi/XSS, verify RBAC rejections, pen-test license key forgery.
7. **Visual Regression:** Playwright responsive snapshots across Mobile, Tablet, 4K Desktop.
8. **Accessibility:** `@axe-core/playwright` automated WCAG AA scans.
9. **Chaos & Recovery:** Programmatically cut network mid-upload during SD card ingestion; assert zero data corruption.

---

### 🚢 Production Release & Delivery Handoff
1. **Monorepo Verification:** Run `turbo run build` and `pnpm run lint:all`. Block release until 0 errors.
2. **Git Release:** Stage code -> Commit -> Push PR -> Auto-merge to `main` -> Tag `v2.0.0-production`.
3. **Cloud & Desktop Deployment:** Deploy CF Workers/D1/R2 via `wrangler`. Compile signed desktop binaries (`.exe`/`.dmg`).
4. **Release Package (`ClickFlash_Release_v2.0/`):**
   - Create `/01_Installation_Manuals` (`1_Ecosystem_Overview.md`, `2_Local_Studio_Setup_Guide.md`, `3_Cloud_Deployment_Guide.md`, `4_Environment_Variables_Cheat_Sheet.md`).
   - Create `/02_User_Manuals` (`Master_Portal_User_Guide.md`, `Touch_Kiosk_Customer_Guide.md`, `Management_Hub_Executive_Guide.md`, `Licensing_Admin_Guide.md`).
   - Place production installers in `/03_Production_Builds` and clean configs in `/04_Assets_and_Config`.

---

### 🚀 Execution Start
1. Create `task.md`.
2. Do not read old reports—immediately use your file reading/grep tools to scan `apps/master` source code from zero.
3. Fix issues, implement features, and run tests autonomously!
</USER_REQUEST>
```
