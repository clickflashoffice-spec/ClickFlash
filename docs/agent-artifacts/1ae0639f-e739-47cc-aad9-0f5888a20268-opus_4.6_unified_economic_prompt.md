# ⚡ Exhaustive & Token-Dense Master Prompt for Claude Opus 4.6

**Copy and paste this complete, un-truncated prompt into Claude Opus 4.6. It collects 100% of the features, routes, security mandates, standalone apps, 9-layer tests, and final delivery instructions from our entire discussion into a structured, token-dense XML format.**

***

```xml
<OPUS_MASTER_ORCHESTRATOR_PROMPT>
<ROLE>Principal Systems Architect, Full-Stack Lead, SDET & Security Officer</ROLE>
<OBJECTIVE>
Execute a 360°, 100% complete audit, feature implementation, UI/UX polish, 9-layer QA gauntlet, and production delivery package for the entire ClickFlash Photography Ecosystem (6 Monorepo Apps + 2 Standalone Infra Apps + Shared Core).
</OBJECTIVE>

<ECONOMIC_EXECUTION_DIRECTIVES>
1. TOKEN EFFICIENCY: Zero fluff/preambles. Output clean code diffs or complete fixed files directly.
2. STATE TRACKING: Maintain `task.md`. Mark `[/]` for active, `[x]` for done. Document milestones in `walkthrough.md`.
3. UNCOMPROMISING MANDATE (100% CUSTOM / NO SUBSCRIPTIONS):
   - NEVER integrate paid third-party SaaS (No Vercel, Auth0, Clerk, Pusher, Algolia, OpenAI, Adobe, or paid analytics).
   - Build custom Auth (JWT/Magic Links), custom WebSockets, custom analytics, custom UI components (Tailwind/Framer).
   - Accepted external infrastructure: Cloudflare Workers/D1/R2, local SQLite/Express, Stripe (payments only).
</ECONOMIC_EXECUTION_DIRECTIVES>

<PER_APP_100_PERCENT_ROUTE_AUDIT_AND_TESTING_MATRIX>
MANDATORY DIRECTIVE: Every single route across all 6 apps MUST undergo a 3-part verification:
  A) Structural Audit: Error boundaries, React 19 Suspense/loading states, 0 dead links, strict TS typing.
  B) Security Audit: Strict Zod input validation, RBAC enforcement (`can("view...")`), XSS/CSRF sanitization.
  C) Dedicated E2E Test: Automated Playwright test verifying 100% of route renders, state transitions, and offline fallbacks.

ROUTE MAP BY APP:
1. `apps/master` (Port 8090):
   - Routes: `/` (Dashboard), `/resort` (LocalResortDashboard), `/albums` (Albums), `/bookings` (Bookings), `/orders` (Orders), `/clients` (Clients), `/photographers` (Photographers), `/settings` (Settings), `/growth` (Growth), `/audit` (System Audit), `/print` (PrintLayout), `/receipt` (CustomerReceipt).
2. `apps/touch` (Port 8091):
   - Views: `welcome` (Screen/Screensaver/RFID Auth), `photos` (Photo Grid Selection), `photo-detail` (Preview/Cart), `order-config` (Checkout Configuration).
3. `apps/management` (Cloud D1 Hub):
   - Views: `executive_dashboard`, `stations_overview`, `orders_sales`, `assets_inventory`, `revenue_income`, `expenses_payroll`, `capital_treasury`, `billing_subscription`, `staff_management`, `session_types`, `reports_insights`, `sync_logs`.
4. `apps/gallery` (Cloud Client Portal):
   - Views: `Gallery` (Proofing/Grid), `Store` (Prints/Products), `Favorites`, `Download`, `Status` (Order Status), `Buy Photos` (MoneyTrash unassigned gallery).
5. `apps/moneytrash` (Port 3000):
   - Routes: `/` (Main Gateway / Drag-and-Drop Ingestor).
6. `apps/website` (Port 3001):
   - Routes: `/`, `/about`, `/blog`, `/bookings`, `/careers`, `/clients`, `/portfolio`, `/pricing`, `/services`, `/contact`, `/faq`, `/privacy`, `/terms`, `/[slug]` (dynamic).
</PER_APP_100_PERCENT_ROUTE_AUDIT_AND_TESTING_MATRIX>

<PART_1_MONOREPO_APPS_AUDIT_AND_FEATURES>
1. `apps/master` (Electron + React 19, Port 8090 | Local Studio Core)
   - NEW CORE FEATURE (100% Custom Offline Auto Photo Editor): Build local WASM/Canvas image processing engine (auto-exposure, color correction, noise reduction, cropping). Hook into `BackgroundJobRunner` for non-blocking bulk edits. Add Before/After toggle slider & album batch presets. Store `edit_metadata` in SQLite and sync to Cloud D1/R2.
   - Resilience & Ops: Ensure `ThermalMonitor` and `BackgroundJobRunner` handle massive high-res photo queues. Audit Express routes & eliminate N+1 SQLite queries. Perfect LAN WebSocket server for Touch Kiosk. Enforce strict Zod RBAC (`can("view...")`). Print layout (`PrintLayout`, `CustomerReceipt`) must be pixel-perfect (`@media print`).

2. `apps/touch` (Electron + React 19, Port 8091 | Offline Customer Tablet)
   - Features: Touch-first UI with glassmorphism, dark mode, and Framer Motion grid-to-preview transitions. 100% local RFID/Wristband scanner & Face biometric login. Persist cart in `localStorage` across reboots. Ensure `Ctrl+Shift+Alt+F12` Admin Override is tamper-proof.

3. `apps/management` (Vite + React 19 + CF Workers/D1 | Global Enterprise Hub)
   - Features: Custom Global vs. Hotel context state selector (no prop drilling). Fleet Monitor live ping system showing online/offline Master nodes. Custom `Cmd+K` command palette. Custom AI Chatbot ("PixelFounder") with local/D1 query engine. Eliminate slow D1 SQL queries.

4. `apps/gallery` (Vite + React 19 + CF Workers/D1/R2/Stripe | Client Portal)
   - Features: Custom Passwordless Auth (Magic Link `?token=`, QR Session, Email/PIN). Native-feeling swipeable Framer Lightbox with edge R2 image delivery. Custom Stripe checkout & webhook fulfillment. Custom Abandoned Cart sync to D1. Optimistic photo proofing (approve/reject) with tactile micro-animations.

5. `apps/moneytrash` (Next.js 16 + Tauri, Port 3000 | Bulk RAW/JPEG Ingestor)
   - Features: Rust multi-threaded ingestion engine for gigabyte-scale SD card folder drops. Chunked streaming upload directly to Cloudflare R2. Custom drag-and-drop UI with granular file/batch progress bars.

6. `apps/website` (Next.js 15 App Router + Tailwind 4, Port 3001 | Marketing Site)
   - Features: 100/100 Lighthouse performance. Custom native dynamic sitemaps, OpenGraph metadata, and lazy-loaded hero assets.

7. Shared Packages (`packages/`)
   - `@clickflash/api` & `@clickflash/validation`: Strict TypeScript contracts & Zod schemas.
   - `@clickflash/logger`: Remove ALL `console.log`; enforce structured logger across all apps.
   - `@clickflash/ui`: Consistent Tailwind design tokens & dark mode across all apps.
</PART_1_MONOREPO_APPS_AUDIT_AND_FEATURES>

<PART_2_STANDALONE_INFRASTRUCTURE_TOOLS>
8. `C:\Users\alamo\Desktop\apps\license-generator` (Offline Security & Licensing)
   - Cryptography: Audit & enforce asymmetric RSA-4096 or Ed25519 cryptographic key signing.
   - Hardware Binding: Lock activation licenses strictly to studio CPU ID, MAC address, or Motherboard UUID.
   - Anti-Tamper: Implement code obfuscation & anti-debugging checks on validation routines. Polish admin license management dashboard.

9. `C:\Users\alamo\Desktop\apps\installer` (Custom Setup & Packaging Wizard)
   - Payload & DevOps: Optimize LZMA/7z bundling compression for fast `.exe`/`.dmg` extraction.
   - Prerequisite Automation: Silently install required dependencies (Node.js, VC++ Redistributables, SQLite drivers).
   - UI & Cleanup: Premium dark-mode setup wizard. Safe uninstaller preserving client databases/photos.
</PART_2_STANDALONE_INFRASTRUCTURE_TOOLS>

<PART_3_NINE_LAYER_PRODUCTION_QA_GAUNTLET>
- Layer 1 (Unit & API Integration): Vitest/Jest suites for `@clickflash/*` packages (95% coverage target). Integration tests for Master Express/SQLite routes and Cloudflare Workers D1/R2 endpoints.
- Layer 2 (Web E2E): Playwright test flows for `management`, `gallery`, `website` (test Magic Links, Stripe test checkouts, context switches).
- Layer 3 (Desktop E2E): Automated Playwright/Tauri binary tests for packaged `master`, `touch`, `moneytrash` (mock RFID scanner inputs).
- Layer 4 (Cross-App Sync Gauntlet):
  - LAN Sync: `Touch Cart Item -> WebSocket -> Master Dashboard Live Update`.
  - Cloud Sync: `Master Offline Edit -> D1/R2 Cloud Sync -> Customer Gallery Store & Management Hub Update`.
- Layer 5 (Load & Stress): `k6` stress test against Master Express (1,000 concurrent photo requests without UI freeze) and Cloudflare Workers (rate limiting & D1 concurrency).
- Layer 6 (Security & Pen-Testing): Scan inputs/`Cmd+K` for SQLi/XSS. Verify strict RBAC role rejections. Pen-test license key forgery/transfer rejection.
- Layer 7 (Visual Regression): Playwright `toHaveScreenshot` responsive snapshot checks across Mobile, Tablet, and 4K Desktop.
- Layer 8 (Accessibility): `@axe-core/playwright` automated WCAG AA scans on Touch Kiosk and Customer Gallery.
- Layer 9 (Chaos & Recovery): Cut network mid-upload during SD card ingestion; assert graceful pause and resume without data corruption.
</PART_3_NINE_LAYER_PRODUCTION_QA_GAUNTLET>

<PART_4_DEVOPS_RELEASE_AND_FINAL_DELIVERY>
1. Build Verification: Ensure `turbo run build` and `pnpm run lint:all` pass with 0 warnings/errors.
2. Git Pipeline: Stage all changes -> Commit with conventional format -> Push & auto-merge PR to `main` -> Tag Git release `v2.0.0-production`.
3. Cloud Deployment: Deploy Cloudflare Workers (D1/R2 bindings) via `wrangler`. Deploy `management`, `gallery`, `website` to live edge URLs.
4. Desktop Packaging: Compile signed `.exe` / `.dmg` desktop binaries for `master`, `touch`, `moneytrash`, and `installer`.
5. Clean Handoff Release Folder (`ClickFlash_Release_v2.0/`):
   - `/01_Installation_Manuals`: Create `1_Ecosystem_Overview.md`, `2_Local_Studio_Setup_Guide.md`, `3_Cloud_Deployment_Guide.md`, `4_Environment_Variables_Cheat_Sheet.md`.
   - `/02_User_Manuals`: Create `Master_Portal_User_Guide.md`, `Touch_Kiosk_Customer_Guide.md`, `Management_Hub_Executive_Guide.md`, `Licensing_Admin_Guide.md`.
   - `/03_Production_Builds`: Place compiled desktop installers and web release artifacts.
   - `/04_Assets_and_Config`: Clean `.env.example`, starter SQLite schema, and branding assets.
</PART_4_DEVOPS_RELEASE_AND_FINAL_DELIVERY>

<EXECUTION_START>
Before writing code, generate `roadmap.md` mapping out how you will systematically execute Parts 1, 2, 3, and 4. Once ready, immediately create `task.md` and start executing Part 1 app-by-app!
</EXECUTION_START>
</OPUS_MASTER_ORCHESTRATOR_PROMPT>
```
