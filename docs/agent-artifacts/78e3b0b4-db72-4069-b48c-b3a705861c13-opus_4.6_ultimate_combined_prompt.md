# ⚡ The Ultimate Opus 4.6 Combined Ecosystem Prompt

**Copy and paste this complete, token-dense prompt into Claude Opus 4.6. It consolidates all features, routes, security mandates, testing layers, infrastructure tool audits, CI/CD pipelines, and final delivery steps across the entire ClickFlash ecosystem into a single structured XML format.**

***

```xml
<OPUS_MASTER_ORCHESTRATOR_PROMPT>
<ROLE>Principal Systems Architect, Full-Stack Lead, SDET, DevOps & Security Officer</ROLE>
<OBJECTIVE>
Execute a 360°, 100% complete audit, feature implementation, UI/UX polish, 9-layer QA gauntlet, CI/CD deployment, infrastructure tooling, and final release packaging for the entire ClickFlash Photography Ecosystem.
</OBJECTIVE>

<ECONOMIC_EXECUTION_DIRECTIVES>
1. TOKEN EFFICIENCY: Zero fluff/preambles. Output clean code diffs or complete fixed files directly.
2. STATE TRACKING: Maintain `task.md`. Mark `[/]` for active, `[x]` for done. Document milestones in `walkthrough.md`.
3. UNCOMPROMISING MANDATE (100% CUSTOM / NO SUBSCRIPTIONS):
   - NEVER integrate paid third-party SaaS (No Vercel, Auth0, Clerk, Pusher, Algolia, OpenAI, Adobe, or paid analytics).
   - Build custom Auth (JWT/Magic Links), WebSockets, analytics, UI components (Tailwind/Framer).
   - Accepted external infrastructure: Cloudflare Workers/D1/R2, local SQLite/Express, Stripe (payments only).
</ECONOMIC_EXECUTION_DIRECTIVES>

<ECOSYSTEM_ARCHITECTURE_CONTEXT>
- Monorepo: Turborepo (Turbo 2.x), PNPM Workspaces.
- Frontend: React 19, TypeScript 5.7, Tailwind CSS 3.x/4, Vite 7.x, Next.js 15/16.
- Desktop: Electron 39.x (Master, Touch), Tauri 2.x (MoneyTrash).
- Backend: Local Express/SQLite (Offline), Cloudflare Workers/D1/R2 (Cloud).
- Comm: LAN WebSockets (Master<->Touch), Custom Cloud Sync (Master->D1).
- Shared Packages: @clickflash/api, config, database, logger, shared, telemetry-web, test-utils, types, ui, validation.
</ECOSYSTEM_ARCHITECTURE_CONTEXT>

<PER_APP_AUDIT_AND_FEATURES_MATRIX>
MANDATORY: Every route must undergo Structural Audit (error boundaries, strict TS), Security Audit (Zod RBAC `can("view...")`, XSS/CSRF sanitization), and E2E Testing.

1. `apps/master` (Port 8090 | Local Studio Core)
   - Routes: `/`, `/resort`, `/albums`, `/bookings`, `/orders`, `/clients`, `/photographers`, `/settings`, `/growth`, `/audit`, `/print`, `/receipt`.
   - Feature: 100% Custom Offline Auto Photo Editor (WASM/Canvas image processing for auto-exposure, color correction, noise reduction, cropping). Hook into `BackgroundJobRunner`. Store `edit_metadata` in SQLite.
   - Core: Eliminate N+1 SQLite queries, perfect LAN WebSocket server, strict Zod RBAC. Pixel-perfect print layout.

2. `apps/touch` (Port 8091 | Offline Customer Tablet)
   - Views: `welcome`, `photos`, `photo-detail`, `order-config`.
   - Feature: Touch-first UI (glassmorphism, Framer Motion transitions). 100% local RFID/Wristband & Face biometric login.
   - Core: Persist cart in `localStorage`. Tamper-proof `Ctrl+Shift+Alt+F12` Admin Override.

3. `apps/management` (Cloud D1 Hub)
   - Views: `executive_dashboard`, `stations_overview`, `orders_sales`, `assets_inventory`, `revenue_income`, `expenses_payroll`, `capital_treasury`, `billing_subscription`, `staff_management`, `session_types`, `reports_insights`, `sync_logs`.
   - Feature: Custom Global vs. Hotel context state selector. Live Fleet Monitor ping system. Custom `Cmd+K` palette. Custom internal AI Chatbot ("PixelFounder").

4. `apps/gallery` (Cloud Client Portal)
   - Views: `Gallery`, `Store`, `Favorites`, `Download`, `Status`, `Buy Photos`.
   - Feature: Custom Passwordless Auth (Magic Link, QR Session, Email/PIN). Native-feeling swipeable Framer Lightbox. Custom Stripe checkout & webhook fulfillment. Custom Abandoned Cart D1 sync. Optimistic proofing (approve/reject).

5. `apps/moneytrash` (Port 3000 | Bulk RAW/JPEG Ingestor)
   - Routes: `/` (Drag-and-Drop Ingestor).
   - Feature: Rust/Tauri multi-threaded ingestion for gigabyte-scale SD drops. Chunked streaming direct to R2. Granular progress bars.

6. `apps/website` (Port 3001 | Marketing Site)
   - Routes: `/`, `/about`, `/blog`, `/bookings`, `/careers`, `/clients`, `/portfolio`, `/pricing`, `/services`, `/contact`, `/faq`, `/privacy`, `/terms`, `/[slug]`.
   - Feature: 100/100 Lighthouse performance. Native dynamic sitemaps, OpenGraph metadata, lazy-loaded hero assets.
</PER_APP_AUDIT_AND_FEATURES_MATRIX>

<INFRASTRUCTURE_TOOLS_AUDIT>
7. `C:\Users\alamo\Desktop\apps\license-generator` (Offline Security & Licensing)
   - Crypto: Asymmetric RSA-4096 or Ed25519 signing.
   - Hardware Binding: Lock to CPU ID, MAC address, or UUID.
   - Polish: Anti-debug checks, obfuscation, admin management dashboard.

8. `C:\Users\alamo\Desktop\apps\installer` (Setup & Packaging Wizard)
   - Payload: Optimize LZMA/7z compression for fast `.exe`/`.dmg` extraction.
   - Prereqs: Silently install Node.js, VC++ Redistributables, SQLite drivers.
   - UX: Premium dark-mode setup wizard. Safe uninstaller preserving client data.
</INFRASTRUCTURE_TOOLS_AUDIT>

<NINE_LAYER_PRODUCTION_QA_GAUNTLET>
1. Unit/Integration: Vitest/Jest for packages (95% coverage target). Express/SQLite & CF Workers D1/R2 API endpoints.
2. Web E2E: Playwright flows for management, gallery, website (Magic Links, Stripe test checkouts).
3. Desktop E2E: Tauri/Electron automated tests for master, touch, moneytrash (mock RFID).
4. Cross-App Sync: LAN (Touch Cart -> Master WS Dashboard) & Cloud (Master Edit -> D1/R2 -> Gallery/Management Update).
5. Load/Stress: `k6` on Master Express (1,000 reqs, BackgroundJobRunner stability) and CF Workers (D1 concurrency, rate limiting).
6. Security/Pen-Test: SQLi/XSS on all search forms. Strict RBAC rejections. Pen-test license key forgery brute force.
7. Visual Regression: Playwright responsive snapshots (Mobile, Tablet, 4K).
8. Accessibility: `@axe-core/playwright` WCAG AA scans (Touch, Gallery).
9. Chaos/Recovery: Cut network mid-upload in MoneyTrash; assert graceful pause and data resume.
</NINE_LAYER_PRODUCTION_QA_GAUNTLET>

<CI_CD_AND_FINAL_DELIVERY_PIPELINE>
1. Monorepo Verification: Grep search secrets. `turbo run build` and `pnpm run lint:all` MUST pass with 0 errors. `turbo run test`.
2. Git Orchestration: Comprehensive conventional commit. Push PR -> Auto-merge to `main` -> Tag `v2.0.0-production`.
3. Cloudflare Global Deployment: `wrangler` deploy edge backend (D1/R2 bindings). Deploy management, gallery, website to Pages. Verify HTTP 200.
4. Desktop Packaging: Trigger `pnpm run make` or `electron-builder` for master/touch. `cargo tauri build` for moneytrash. Compile and code-sign all binaries.
5. Live Prod E2E: Run `pnpm run test:e2e:prod` targeting live URLs (Master syncs to Prod D1 -> displays in Prod Gallery -> Stripe checkout).
6. Clean Handoff Release Folder (`ClickFlash_Release_v2.0/`):
   - `/01_Installation_Manuals`: `1_Ecosystem_Overview.md`, `2_Local_Studio_Setup_Guide.md`, `3_Cloud_Deployment_Guide.md`, `4_Environment_Variables_Cheat_Sheet.md`.
   - `/02_User_Manuals`: `Master_Portal_User_Guide.md`, `Touch_Kiosk_Customer_Guide.md`, `Management_Hub_Executive_Guide.md`, `Licensing_Admin_Guide.md`.
   - `/03_Production_Builds`: Compiled desktop installers (.exe/.dmg) and web artifacts.
   - `/04_Assets_and_Config`: Clean `.env.example`, starter SQLite, branding assets.
   - `/README.md`: Master table of contents.
</CI_CD_AND_FINAL_DELIVERY_PIPELINE>

<EXECUTION_START>
Before writing code, generate `roadmap.md` mapping out how you will systematically execute the PER_APP_AUDIT, INFRASTRUCTURE_TOOLS, 9-LAYER_QA, and CI_CD_DELIVERY phases. Once approved, immediately create `task.md` and start executing autonomously!
</EXECUTION_START>
</OPUS_MASTER_ORCHESTRATOR_PROMPT>
```
