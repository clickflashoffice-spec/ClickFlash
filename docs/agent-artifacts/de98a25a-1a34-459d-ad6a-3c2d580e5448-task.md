# Task Tracking: 100% Custom Offline Automatic Photo Editor

- [ ] **Phase 1: Interactive Frontend Editor (`apps/master`)**
  - [ ] Complete `PhotoEditor.tsx` real-time preview integration.
  - [ ] Add sliders for Manual Edits (Exposure, Contrast, Highlights, Shadows).
  - [x] Connect Save action to `apiService`.
  - [x] Implement `BatchControls.tsx` UI for bulk applying edits.
  - [x] Improve `BeforeAfterSlider.tsx` for pixel-perfect comparison.
- [ ] **Phase 2: High-Performance Backend Rendering Engine**
  - [x] Modify `backend/workers/photoWorker.ts` to fully process the `apply-edits` job (baking highres/preview).
  - [x] Update `backend/server.ts` to expose `/api/photos/batch-### UI Review Canvas Integration
- [x] Create the generic `PhotoEditor` wrapper component for the review canvas (supports zooming, panning, before/after).
- [x] Embed the Auto-Editor UI inside the `AlbumEditor` when the user clicks "Review Auto Edits".
- [x] Wire up the "Accept" and "Reject" flow to transition edits from `autoEdits` to `manualEdits`.
- [x] Update `VirtualizedFilmstrip` thumbnail components to visually indicate `autoEnhanced` state.

### E2E Flow / Backend
- [x] Ensure `collections.ts` batch edit endpoint persists `autoEdits` and `autoEnhanced` correctly.
- [x] Perform integration test of the save flow (Confirm & Close -> Save Batch -> UI updates).rs correctly).
- [ ] **Phase 3: Database Storage and Sync**
  - [ ] Add `manualEdits` JSON to SQLite `photos` schema (if not present).
  - [ ] Implement DB methods in `services/db.ts` to read/write `manualEdits`.
- [ ] **Phase 4: Integration and E2E Testing**
  - [ ] Create `e2e/offline-editor.spec.ts` testing the photo edit pipeline.
  - [ ] Perform manual visual verifications (sliders work, backend renders correctly).
