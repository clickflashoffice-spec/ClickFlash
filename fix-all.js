const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory() && !file.includes('node_modules') && !file.includes('dist')) { results = results.concat(walk(file)); } else if (file.endsWith('.json') && file.includes('tsconfig')) { results.push(file); } }); return results; } const files = [...walk('packages'), ...walk('apps')]; files.forEach(f => { let c = fs.readFileSync(f, 'utf8'); if (!c.includes('"types":') && c.includes('"compilerOptions"')) { if (c.includes('"node"') || f.includes('server') || f.includes('config') || f.includes('cli') || f.includes('electron') || f.includes('main') || f.includes('utils')) { c = c.replace(/"compilerOptions"\s*:\s*\{/, '"compilerOptions": {\n    "types": ["node"],'); } else { c = c.replace(/"compilerOptions"\s*:\s*\{/, '"compilerOptions": {\n    "types": [],'); } fs.writeFileSync(f, c); console.log('Fixed', f); } });
